# Решение проблемы "Ошибка загрузки Stats"

## 🔍 Диагностика

### Шаг 1: Откройте консоль браузера (F12)

Найдите сообщения с `[Google Sheets]` и скопируйте их. Особенно важны:
- Сообщения об ошибках
- HTTP статусы
- Ответы от API

### Шаг 2: Проверьте подключение

В консоли браузера выполните:

```javascript
checkGoogleSheets()
```

Это покажет детальную информацию о каждом листе.

### Шаг 3: Проверьте API напрямую

Откройте в браузере этот URL (ваш Script URL):

```
https://script.google.com/macros/s/AKfycbzcgBWWM0gXGIKxgrpks0PspsSGBNk3KZIj-hXG0g3449E7-X1qWMBFVWVkXq3Kogd9/exec?path=/api/Stats
```

**Что должно быть:**
- Если видите JSON с данными → API работает, проблема в данных
- Если видите ошибку → проблема в Google Apps Script или структуре листа

## 🔧 Частые проблемы и решения

### Проблема 1: "Sheet 'Stats' not found"

**Причина:** Лист не существует или называется неправильно

**Решение:**
1. Откройте ваш Google Sheet
2. Убедитесь, что есть лист с названием **Stats** (точно так, с большой буквы)
3. Если листа нет - создайте его
4. Если название другое - переименуйте в **Stats**

### Проблема 2: "Failed to fetch" или CORS ошибка

**Причина:** В режиме разработки (localhost) браузер блокирует запросы к другому домену (script.google.com) из‑за CORS.

**Решение (автоматическое):** В проекте настроен **прокси Vite**: при `npm run dev` все запросы к Google Sheets идут через ваш сервер разработки (`/api-google`), поэтому CORS не возникает. Убедитесь, что в `.env` задан `VITE_GOOGLE_SCRIPT_URL` и **перезапустите** dev‑сервер после изменения `.env`.

**Если CORS всё равно появляется:** проверьте, что Google Apps Script развернут правильно:

1. Откройте Google Sheet → **Extensions** → **Apps Script**
2. Убедитесь, что код из `backend/GoogleAppsScript.gs` вставлен
3. **Deploy** → **Manage deployments**
4. Если нет deployment - создайте новый:
   - **Deploy** → **New deployment**
   - Тип: **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone** ⚠️ ВАЖНО!
   - **Deploy**
5. Скопируйте новый URL и обновите в `.env`

### Проблема 3: Пустой ответ (0 строк)

**Причина:** Лист пустой или неправильная структура

**Решение:**
1. Откройте лист **Stats** в Google Sheet
2. Проверьте первую строку - должны быть заголовки:
   ```
   id | value | label | icon | order
   ```
3. Данные начинаются со второй строки:
   ```
   1 | 50+ | Projects Delivered | | 1
   2 | 98% | Client Satisfaction | | 2
   3 | 10+ | Years Experience | Zap | 3
   ```

### Проблема 4: HTTP 404 или другие ошибки

**Причина:** Неправильный URL или проблемы с deployment

**Решение:**
1. Проверьте URL в `.env` - должен быть полный URL Web app
2. Убедитесь, что deployment активен
3. Попробуйте создать новый deployment

## ✅ Пошаговая проверка

1. **Проверьте .env файл:**
   ```env
   VITE_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/.../exec
   VITE_GOOGLE_SHEET_ID=...
   ```

2. **Проверьте Google Sheet:**
   - Лист "Stats" существует?
   - Первая строка = заголовки?
   - Есть данные во второй строке и далее?

3. **Проверьте Google Apps Script:**
   - Код вставлен?
   - Deployment создан?
   - "Who has access" = "Anyone"?

4. **Проверьте в браузере:**
   - Откройте URL API напрямую
   - Что возвращается?

## 🧪 Тестирование

Выполните в консоли браузера:

```javascript
// 1. Проверить переменные
console.log('Script URL:', import.meta.env.VITE_GOOGLE_SCRIPT_URL);
console.log('Sheet ID:', import.meta.env.VITE_GOOGLE_SHEET_ID);

// 2. Проверить подключение
checkGoogleSheets();

// 3. Тест конкретного листа
fetch('https://script.google.com/macros/s/AKfycbzcgBWWM0gXGIKxgrpks0PspsSGBNk3KZIj-hXG0g3449E7-X1qWMBFVWVkXq3Kogd9/exec?path=/api/Stats')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);
```

## 📋 Структура листа Stats

Убедитесь, что лист Stats имеет такую структуру:

| id | value | label | icon | order |
|----|-------|-------|------|-------|
| 1  | 50+   | Projects Delivered | | 1 |
| 2  | 98%   | Client Satisfaction | | 2 |
| 3  | 10+   | Years Experience | Zap | 3 |

**Важно:**
- Первая строка = заголовки (обязательно!)
- Данные со второй строки
- Колонки должны называться точно так

## 🆘 Если ничего не помогает

1. Создайте новый deployment в Google Apps Script
2. Обновите URL в `.env`
3. Перезапустите проект (`npm run dev`)
4. Проверьте консоль браузера на новые ошибки
5. Скопируйте все сообщения из консоли и отправьте мне
