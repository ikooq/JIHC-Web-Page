# Google Sheets CMS - Система управления контентом

Теперь весь контент вашего сайта управляется через Google Sheets! Вы можете редактировать тексты, добавлять услуги, отзывы, кейсы и другую информацию прямо в таблице.

## ✅ Что было сделано

1. ✅ Создана система загрузки данных из Google Sheets
2. ✅ Обновлены компоненты для использования данных из Google Sheets:
   - HeroSection (главная секция)
   - ServicesSection (услуги)
   - TestimonialsSection (отзывы)
   - CasesSection (кейсы)
   - Stats (статистика)
3. ✅ Созданы хуки для удобной работы с данными
4. ✅ Добавлена документация по структуре Google Sheets

## 🚀 Быстрый старт

### 1. Создайте Google Sheet

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новый документ
3. Создайте листы с названиями:
   - **Hero** - главная секция
   - **Services** - услуги
   - **Testimonials** - отзывы
   - **Cases** - кейсы
   - **Stats** - статистика

### 2. Настройте структуру листов

Подробная инструкция по структуре каждого листа находится в файле:
**`backend/GOOGLE_SHEETS_STRUCTURE.md`**

### 3. Настройте Google Apps Script

1. Откройте ваш Google Sheet
2. **Extensions** → **Apps Script**
3. Вставьте код из `backend/GoogleAppsScript.gs`
4. **Deploy** → **New deployment** → **Web app**
5. Установите "Who has access: **Anyone**"
6. Скопируйте Web App URL

### 4. Настройте переменные окружения

Откройте файл `.env` и добавьте:

```env
VITE_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
VITE_GOOGLE_SHEET_ID=YOUR_GOOGLE_SHEET_ID
```

**Где найти Sheet ID:**
- URL вашего Google Sheet: `https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit`
- Скопируйте `[SHEET_ID]` и вставьте в `.env`

### 5. Заполните данные в Google Sheets

Следуйте инструкциям в `backend/GOOGLE_SHEETS_STRUCTURE.md` для заполнения каждого листа.

### 6. Перезапустите проект

```bash
npm run dev
```

## 📝 Как редактировать контент

1. Откройте ваш Google Sheet
2. Найдите нужный лист (Hero, Services, Testimonials, Cases, Stats)
3. Отредактируйте данные в таблице
4. Сохраните (автоматически)
5. Обновите страницу сайта (F5) - изменения появятся сразу!

## 📚 Документация

- **Структура Google Sheets:** `backend/GOOGLE_SHEETS_STRUCTURE.md`
- **Инструкция по настройке:** `backend/SETUP_INSTRUCTIONS.md`
- **API документация:** `backend/API_EXAMPLES.md`

## 🔧 Технические детали

### Хуки для работы с данными

```typescript
// Загрузить все данные из листа
const { data, loading, error } = useGoogleSheetsData({
  sheetName: "Services",
});

// Загрузить одну запись по ID
const { data, loading, error } = useGoogleSheetsItem("Services", 1);

// Загрузить по полю field
const { data, loading, error } = useGoogleSheetsByField("Hero", "main");
```

### Утилиты

```typescript
import { fetchSheetData, fetchSheetItem, fetchSheetByField } from "@/lib/googleSheetsCMS";

// Загрузить все данные
const services = await fetchSheetData("Services");

// Загрузить одну запись
const hero = await fetchSheetByField("Hero", "main");
```

## ⚠️ Важные замечания

- ✅ Первая строка каждого листа = названия колонок (обязательно!)
- ✅ Данные начинаются со второй строки
- ✅ Названия листов чувствительны к регистру
- ✅ Для булевых значений используйте `TRUE`/`FALSE`
- ✅ Для переноса строки используйте `\n` в тексте

## 🎯 Что можно редактировать через Google Sheets

- ✅ Заголовки и описания на главной странице
- ✅ Услуги (FinTech, Healthcare)
- ✅ Отзывы клиентов
- ✅ Кейсы/портфолио проектов
- ✅ Статистика (проекты, удовлетворенность, опыт)
- ✅ Тексты кнопок и ссылки

## 🆘 Если что-то не работает

1. Проверьте, что Google Apps Script развернут как Web app
2. Проверьте, что "Who has access" = "Anyone"
3. Проверьте переменные окружения в `.env`
4. Проверьте консоль браузера (F12) на ошибки
5. Убедитесь, что структура листов соответствует документации

## 📞 Поддержка

Если возникли проблемы, проверьте:
- `backend/SETUP_INSTRUCTIONS.md` - пошаговая инструкция
- `backend/GOOGLE_SHEETS_STRUCTURE.md` - структура данных
- Консоль браузера для ошибок

---

**Готово!** Теперь вы можете управлять всем контентом сайта через Google Sheets! 🎉
