# Отладка подключения к Google Sheets

Если данные не загружаются из Google Sheets, следуйте этой инструкции.

## Шаг 1: Проверьте консоль браузера (F12)

Откройте консоль браузера (F12) и найдите сообщения, начинающиеся с `[Google Sheets]`:

- ✅ `[Google Sheets] Loaded X rows from Hero` - данные загружены успешно
- ⚠️ `[Google Sheets] Skipping Hero` - URL не настроен
- ❌ `[Google Sheets] Error fetching Hero` - ошибка загрузки

## Шаг 2: Проверьте переменные окружения

В консоли браузера выполните:

```javascript
console.log('Script URL:', import.meta.env.VITE_GOOGLE_SCRIPT_URL);
console.log('Sheet ID:', import.meta.env.VITE_GOOGLE_SHEET_ID);
```

Оба значения должны быть заполнены.

## Шаг 3: Проверьте Google Apps Script

1. Откройте ваш Google Sheet
2. **Extensions** → **Apps Script**
3. Нажмите **Run** → выберите функцию `doGet`
4. Проверьте, нет ли ошибок

## Шаг 4: Проверьте Web App Deployment

1. В Google Apps Script: **Deploy** → **Manage deployments**
2. Убедитесь, что:
   - ✅ Есть активный deployment
   - ✅ "Who has access" = **Anyone**
   - ✅ URL скопирован правильно в `.env`

## Шаг 5: Тестируйте API напрямую

Откройте в браузере (замените YOUR_SCRIPT_ID на ваш):

```
https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?path=/api/Hero
```

Должен вернуться JSON с данными или ошибкой.

## Шаг 6: Проверьте структуру Google Sheet

Убедитесь, что:

1. ✅ Листы называются точно: **Hero**, **Services**, **Testimonials**, **Cases**, **Stats**
2. ✅ Первая строка каждого листа содержит названия колонок
3. ✅ Данные начинаются со второй строки
4. ✅ В листе Hero есть строка с `field = "main"`

## Шаг 7: Проверьте права доступа

1. Откройте ваш Google Sheet
2. Нажмите **Share** (Поделиться)
3. Убедитесь, что доступ есть у всех, кому нужен (или установите "Anyone with the link can view")

## Шаг 8: Проверьте логи в консоли

После перезагрузки страницы в консоли должны появиться:

```
🚀 Running Google Sheets connection test...
🔍 Testing Google Sheets connection...
📋 Script URL: https://script.google.com/...
📊 Testing Hero...
   Status: 200 OK
   ✅ Found X rows
```

## Частые проблемы

### Проблема: "Skipping Hero: hasScriptUrl: false"

**Решение:** Проверьте файл `.env` - переменная `VITE_GOOGLE_SCRIPT_URL` должна быть заполнена.

### Проблема: "Error fetching Hero: Failed to fetch"

**Решение:** 
- Проверьте, что Google Apps Script развернут как Web app
- Проверьте, что "Who has access" = "Anyone"
- Проверьте URL в `.env`

### Проблема: "Sheet 'Hero' not found"

**Решение:**
- Убедитесь, что лист называется точно **Hero** (с большой буквы)
- Проверьте, что лист существует в Google Sheet

### Проблема: "Found 0 rows"

**Решение:**
- Проверьте, что данные начинаются со второй строки
- Проверьте, что первая строка содержит названия колонок
- Убедитесь, что есть данные в листе

### Проблема: Данные не обновляются

**Решение:**
- Обновите страницу (F5)
- Проверьте, что изменения сохранены в Google Sheet
- Убедитесь, что используется правильный deployment (может быть старая версия)

## Тестирование API вручную

Выполните в консоли браузера:

```javascript
// Тест загрузки Hero
fetch('https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?path=/api/Hero')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);

// Тест загрузки Services
fetch('https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?path=/api/Services')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);
```

Замените `YOUR_SCRIPT_ID` на ваш реальный ID из `.env`.

## Если ничего не помогает

1. Проверьте все шаги выше еще раз
2. Убедитесь, что Google Apps Script код обновлен (из файла `GoogleAppsScript.gs`)
3. Создайте новый deployment в Google Apps Script
4. Проверьте консоль браузера на ошибки CORS
5. Убедитесь, что `.env` файл находится в корне проекта
